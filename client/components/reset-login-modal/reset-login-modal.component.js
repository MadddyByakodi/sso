import template from './reset-login-modal.pug';
import controller from './reset-login-modal.controller';

const ResetLoginModalComponent = {
  template,
  controller,
};

export default ResetLoginModalComponent;
